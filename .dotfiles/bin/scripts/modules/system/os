#!/bin/bash

# Realiza o update o Sistema Operacional
function updateos(){
    yes|sudo apt update
    yes|sudo apt upgrade
    yes|sudo apt dist-upgrade
    yes|sudo apt autoremove
    yes|sudo apt autoclean
    yes|sudo apt clean
}
